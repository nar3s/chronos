export interface StudyTopic {
  id: string;
  label: string;
  color: string;
  active: boolean;
}

export interface WorkoutType {
  id: string;
  label: string;
  color: string;
  isRestDay: boolean;
}
